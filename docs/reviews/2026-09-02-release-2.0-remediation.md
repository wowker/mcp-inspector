# MCP Inspector 2.0 Release Review Remediation

## Status

- Date: 2026-09-02
- Scope: remediation of every Critical and Required finding in the first independent review and its follow-up review
- Release action: none; the package version, Git tag, and npm registry were not changed
- Independent re-review: approved with 0 Critical, 0 Required, 0 Recommended, and 0 new findings; see `2026-09-02-release-2.0-independent-rereview.md`

## Finding closure

| Finding | Remediation | Evidence |
|---|---|---|
| C1 | Replaced the reusable URL/session-storage credential with a 60-second, single-use bootstrap ticket. Successful exchange sets a host-only `HttpOnly`, `SameSite=Strict`, `/api` cookie and OAuth returns without a session query. | Session auth, bootstrap, OAuth return, client bootstrap, and main entry regression tests |
| C2 | Rejects credential-shaped connection query parameters, including compact aliases such as `auth`, `key`, `sig`, `access_key`, and suffix forms such as `tenantCredential`. Ordinary routing parameters remain supported. URL fragments are rejected; legacy URL display, HTTP observation, errors, and export boundaries are independently sanitized. | Sensitive URL, connection, observed-fetch, route, and export tests |
| R1 | Default Server export v2 excludes Tab drafts, Run request/response/event payloads, and saved request/response items. The manifest explicitly records the omitted scopes. | Shared export contract, server export, and client decoder tests |
| R2 | Added a representative data-bearing schema 11→18 migration fixture while retaining schema 13→18 automated-testing coverage. | `release-migration-matrix.test.ts` |
| R3 | Added pure None authentication and real same-URL OAuth/Bearer isolation using distinct connection IDs. The fixture closes the test response connection so a restarted same-origin endpoint cannot inherit a stale pooled socket. | Streamable MCP integration tests |
| R4 | Large Run detail responses are read as transferable `ArrayBuffer` data, decoded and schema-validated in a dedicated module Worker, avoiding a full main-thread `response.text()` copy. A production E2E starts a real MCP fixture, returns 10 MiB structured JSON, verifies the Worker asset is requested, waits until the result pane is interactive, and asserts both Long Task and timer-drift budgets below 100 ms. | Run API client tests and `large-json-response.spec.ts` |
| R5 | Replaced directory-prefix acceptance with exact runtime files plus constrained hashed client-asset and numbered-migration patterns. Release verification builds, validates, hashes, and retains one immutable `.release/package.tgz`; Make publishes that exact file. | Release artifact policy tests and artifact verification |
| R6 | The English production smoke creates a project, configures/connects a real MCP Server, opens a Tool, edits arguments, executes it, and inspects the response. The nested object and top-level parameter editors now source `Form` and `Raw JSON` labels through the paired locale resources. | English E2E, editor tests, and locale tree tests |
| R7 | Added axe WCAG A/AA checks without ToolTree or debug-Tab exclusions, plus stable zh-CN/en-US × light/dark screenshots. Theme checks run with reduced motion; only dynamic Run header and request/response payload regions are masked. | Core debugger and i18n E2E snapshots |
| R8 | Added linked tab/tabpanels, roving Tab focus, Arrow/Home/End behavior, accessible folder-menu navigation, shared focus-trapped Dialogs, Escape/outside close, focus restoration, and menu dismissal on both Tab and Shift+Tab without blocking native focus movement. | Debug workspace, parameter editor, ToolTree, and Dialog tests |

## Additional recommended work completed

- The 1,000-Tool production fixture keeps at most 200 rows mounted, searches the complete catalog within the 100 ms budget, verifies the catalog remains the sole scrolling owner, and asserts continuous-scroll Long Task and frame-gap budgets below 100 ms.
- Release verification creates one tgz, validates and hashes that exact artifact, and the Make publish target consumes it rather than packing again.

## Final implementer verification

- `npm run verify`: passed
  - TypeScript client/server typecheck: passed
  - Vitest: 118 files, 826 tests passed
  - Production Playwright: 7 tests passed
- `npm run verify:release-artifacts`: passed
  - Initial JavaScript: 150.10 KiB gzip (budget 220 KiB)
  - Initial CSS: 49.76 KiB gzip (budget 70 KiB)
  - SQLite migrations: 18 source/dist byte-matched files
  - npm artifact: 32 allowlisted files
  - Artifact: `.release/package.tgz`
  - SHA-256: `6a8649023f067ee92fee3d162177d0f4133d404efcf6e81fa80f0f1d1983676f`
- `git diff --check`: passed

The build still emits Vite's advisory warning for lazy chunks larger than 500 kB. The initial-route gzip budgets pass, and this warning is not treated as a release failure by the documented gate.
